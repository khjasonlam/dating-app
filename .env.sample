# Database Configuration
# このファイルをコピーして.envファイルを作成し、実際の値を設定してください
# cp .env.sample .env

DB_HOST=localhost
DB_NAME=datingAppDB
DB_USER=your_username
DB_PASS=your_password

# Application Settings
# DEBUG_MODE=true

# 注意事項:
# - .envファイルは.gitignoreに含まれているため、Gitにはコミットされません
# - 本番環境では必ず実際の認証情報を使用してください
# - このファイル（.env.sample）はリポジトリにコミットされます
